const { MessageEmbed } = require("discord.js");
const config = require("../../config.json");
module.exports = {
  name: "",
  category: "Moderation",
  description: "",
  run: async (client, message, args) => {

  }} 